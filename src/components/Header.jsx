import React from 'react';

const Header = () => {
  return (
    
      <div className="logo" style={{
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        gap: '5px',
        marginBottom: '10px'
      }}>
      </div>
      
  );
};

export default Header;